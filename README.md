# Doca releases

Public distribution of Doca app binaries.
Managed automatically by `electron-builder --publish always` from the main Doca repo.

Do not commit code here — this repo exists solely for releases.
